<!DOCTYPE html>
<html lang="pt-BR" class="h-full">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Portal de Certificados - Jornada de Cardiologia da Fronteira</title>

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              cardio: {
                50: "#fef2f2",
                100: "#fee2e2",
                200: "#fecaca",
                300: "#fca5a5",
                400: "#f87171",
                500: "#ef4444",
                600: "#dc2626",
                700: "#b91c1c",
                800: "#991b1b",
                900: "#7f1d1d",
              },
            },
            animation: {
              "fade-in": "fadeIn 0.5s ease-in-out",
              "slide-up": "slideUp 0.5s ease-out",
              "bounce-in": "bounceIn 0.6s ease-out",
              "pulse-slow": "pulse 3s ease-in-out infinite",
              heartbeat: "heartbeat 1.5s ease-in-out infinite",
            },
            keyframes: {
              fadeIn: {
                "0%": { opacity: "0" },
                "100%": { opacity: "1" },
              },
              slideUp: {
                "0%": { transform: "translateY(100px)", opacity: "0" },
                "100%": { transform: "translateY(0)", opacity: "1" },
              },
              bounceIn: {
                "0%": { transform: "scale(0.3)", opacity: "0" },
                "50%": { transform: "scale(1.05)" },
                "70%": { transform: "scale(0.9)" },
                "100%": { transform: "scale(1)", opacity: "1" },
              },
              heartbeat: {
                "0%, 50%, 100%": { transform: "scale(1)" },
                "25%, 75%": { transform: "scale(1.1)" },
              },
            },
          },
        },
      };
    </script>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, sans-serif;
      }

      .event-header {
        background: linear-gradient(135deg, #dc2626 0%, #991b1b 100%);
        position: relative;
        overflow: hidden;
      }

      .gradient-bg {
        background: linear-gradient(135deg, #dc2626 0%, #991b1b 100%);
      }

      .card-shadow {
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1),
          0 10px 10px -5px rgba(0, 0, 0, 0.04);
      }

      .input-focus:focus {
        box-shadow: 0 0 0 3px rgba(220, 38, 38, 0.1);
      }

      .btn-hover:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
      }

      .floating-shapes {
        position: absolute;
        width: 100%;
        height: 100%;
        overflow: hidden;
        pointer-events: none;
      }

      .shape {
        position: absolute;
        opacity: 0.1;
        animation: float 6s ease-in-out infinite;
      }

      .shape:nth-child(1) {
        top: 10%;
        left: 10%;
        animation-delay: 0s;
      }
      .shape:nth-child(2) {
        top: 20%;
        right: 10%;
        animation-delay: 1s;
      }
      .shape:nth-child(3) {
        bottom: 10%;
        left: 20%;
        animation-delay: 2s;
      }
      .shape:nth-child(4) {
        bottom: 20%;
        right: 20%;
        animation-delay: 3s;
      }

      @keyframes float {
        0%,
        100% {
          transform: translateY(0px) rotate(0deg);
        }
        50% {
          transform: translateY(-20px) rotate(180deg);
        }
      }

      .glassmorphism {
        background: rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
      }

      .heart-pattern {
        background-image: url("data:image/svg+xml,%3Csvg width='40' height='40' viewBox='0 0 40 40' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='%23fee2e2' fill-opacity='0.3'%3E%3Cpath d='M20 30c-8-8-15-15-15-20 0-5 4-8 8-8s8 3 8 8-7 12-15 20z'/%3E%3C/g%3E%3C/svg%3E");
      }
    </style>
  </head>
  <body class="h-full bg-neutral-50 heart-pattern">
    <!-- Floating Shapes Background -->
    <div class="floating-shapes">
      <div class="shape w-32 h-32 bg-red-500 rounded-full"></div>
      <div class="shape w-24 h-24 bg-pink-500 rounded-full"></div>
      <div class="shape w-40 h-40 bg-red-600 rounded-full"></div>
      <div class="shape w-28 h-28 bg-rose-500 rounded-full"></div>
    </div>

    <!-- Header -->
    <header class="event-header text-white relative z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center py-6">
          <div class="flex items-center">
            <a href="/" class="flex items-center cursor-pointer">
              <div class="gradient-bg p-4 rounded-2xl mr-4">
                <i
                  class="fas fa-heartbeat text-white text-3xl animate-heartbeat"
                ></i>
              </div>
              <div>
                <h1 class="text-3xl font-bold">Jornada de Cardiologia</h1>
                <p class="text-red-100 text-sm">da Fronteira - Edição 2025</p>
                <p class="text-red-200 text-xs">Bagé - Rio Grande do Sul</p>
              </div>
            </a>
          </div>
        </div>
      </div>
    </header>

    <!-- Hero Section -->
    <section class="relative py-20 px-4 sm:px-6 lg:px-8">
      <div class="max-w-4xl mx-auto text-center">
        <!-- Alert Messages -->
        <div
          id="error-alert"
          class="hidden mb-8 bg-red-50 border border-red-200 rounded-xl p-4 alert-auto-hide animate-slide-up"
        >
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <i class="fas fa-exclamation-triangle text-red-400 text-lg"></i>
            </div>
            <div class="ml-3">
              <p class="text-red-800 font-medium" id="error-message"></p>
            </div>
          </div>
        </div>

        <div
          id="success-alert"
          class="hidden mb-8 bg-green-50 border border-green-200 rounded-xl p-4 alert-auto-hide animate-slide-up"
        >
          <div class="flex items-center">
            <div class="flex-shrink-0">
              <i class="fas fa-check-circle text-green-400 text-lg"></i>
            </div>
            <div class="ml-3">
              <p class="text-green-800 font-medium" id="success-message"></p>
            </div>
          </div>
        </div>

        <!-- Main Content -->
        <div class="animate-fade-in">
          <div class="mb-8">
            <div
              class="inline-flex items-center bg-red-50 text-red-600 px-4 py-2 rounded-full text-sm font-medium mb-6"
            >
              <i class="fas fa-certificate mr-2"></i>
              Jornada de Cardiologia da Fronteira
            </div>
            <h1
              class="text-5xl md:text-6xl font-bold text-neutral-900 mb-6 leading-tight"
            >
              Encontre seu
              <span
                class="text-transparent bg-clip-text bg-gradient-to-r from-red-600 to-red-800"
              >
                Certificado
              </span>
            </h1>
            <p
              class="text-xl text-neutral-600 mb-8 max-w-2xl mx-auto leading-relaxed"
            >
              Digite seu CRM para localizar e baixar seu certificado de
              participação da Jornada de Cardiologia da Fronteira 2025.
            </p>
          </div>

          <!-- Event Info Card -->
          <div
            class="max-w-md mx-auto mb-8 bg-white rounded-xl p-6 card-shadow animate-bounce-in"
          >
            <div class="flex items-center justify-center mb-4">
              <div
                class="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center"
              >
                <i class="fas fa-calendar-alt text-red-600 text-2xl"></i>
              </div>
            </div>
            <h3 class="text-lg font-semibold text-neutral-900 mb-2">
              Evento Realizado
            </h3>
            <p class="text-neutral-600 text-sm mb-2">
              04 e 05 de julho de 2025
            </p>
            <p class="text-red-600 font-medium">
              Associação Rural de Bagé - RS
            </p>
          </div>

          <!-- Search Form -->
          <div class="max-w-md mx-auto animate-bounce-in">
            <form
              id="search-form"
              action="/buscar"
              method="POST"
              class="space-y-6"
            >
              <div class="relative">
                <div
                  class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none"
                >
                  <i class="fas fa-id-card text-neutral-400 text-lg"></i>
                </div>

                <!-- Ely Neto: Atualizei a linha do pattern pra garantir a questão do REGEX:
                -->
               <input
                  type="text"
                  name="crm"
                  id="crm"
                  placeholder="Digite seu CRM (5 ou 6 dígitos)"
                  pattern="^[0-9]{5,6}$"
                  minlength="5"
                  maxlength="6"
                  required
                  class="w-full pl-12 pr-4 py-4 text-lg border border-neutral-300 rounded-xl focus:ring-2 focus:ring-red-500 focus:border-transparent input-focus transition-all duration-200 bg-white card-shadow"
                />
              </div>
              <button
                type="submit"
                class="w-full bg-gradient-to-r from-red-600 to-red-700 hover:from-red-700 hover:to-red-800 text-white font-semibold py-4 px-6 rounded-xl transition-all duration-200 btn-hover card-shadow cursor-pointer"
              >
                <i class="fas fa-search mr-2"></i>
                Buscar Certificado
              </button>
            </form>
          </div>

          <!-- Info Cards -->
          <div
            class="grid grid-cols-1 md:grid-cols-3 gap-6 mt-16 animate-slide-up"
          >
            <div
              class="bg-white p-6 rounded-xl card-shadow hover:shadow-lg transition-shadow duration-300"
            >
              <div
                class="w-12 h-12 bg-red-100 rounded-lg flex items-center justify-center mb-4 mx-auto"
              >
                <i class="fas fa-shield-alt text-red-600 text-xl"></i>
              </div>
              <h3 class="text-lg font-semibold text-neutral-900 mb-2">
                Seguro
              </h3>
              <p class="text-neutral-600">
                Certificados protegidos com criptografia avançada e validação
                rigorosa.
              </p>
            </div>

            <div
              class="bg-white p-6 rounded-xl card-shadow hover:shadow-lg transition-shadow duration-300"
            >
              <div
                class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center mb-4 mx-auto"
              >
                <i class="fas fa-download text-green-600 text-xl"></i>
              </div>
              <h3 class="text-lg font-semibold text-neutral-900 mb-2">
                Rápido
              </h3>
              <p class="text-neutral-600">
                Download instantâneo em formato PDF de alta qualidade.
              </p>
            </div>

            <div
              class="bg-white p-6 rounded-xl card-shadow hover:shadow-lg transition-shadow duration-300"
            >
              <div
                class="w-12 h-12 bg-red-100 rounded-lg flex items-center justify-center mb-4 mx-auto"
              >
                <i class="fas fa-certificate text-red-600 text-xl"></i>
              </div>
              <h3 class="text-lg font-semibold text-neutral-900 mb-2">
                Válido
              </h3>
              <p class="text-neutral-600">
                Certificados oficiais com validade reconhecida nacionalmente.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section class="py-16 bg-white relative z-10">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
          <h2 class="text-3xl font-bold text-neutral-900 mb-4">
            Como funciona?
          </h2>
          <p class="text-lg text-neutral-600">
            Processo simples e seguro em 3 passos
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          <div class="text-center">
            <div
              class="w-16 h-16 bg-gradient-to-r from-red-500 to-red-700 rounded-full flex items-center justify-center mx-auto mb-4"
            >
              <span class="text-white text-xl font-bold">1</span>
            </div>
            <h3 class="text-xl font-semibold text-neutral-900 mb-2">
              Digite seu CRM
            </h3>
            <p class="text-neutral-600">
              Insira seu número de CRM com 6 dígitos no campo de busca.
            </p>
          </div>

          <div class="text-center">
            <div
              class="w-16 h-16 bg-gradient-to-r from-red-500 to-red-700 rounded-full flex items-center justify-center mx-auto mb-4"
            >
              <span class="text-white text-xl font-bold">2</span>
            </div>
            <h3 class="text-xl font-semibold text-neutral-900 mb-2">
              Validação
            </h3>
            <p class="text-neutral-600">
              Sistema valida automaticamente seu CRM em nossa base de dados.
            </p>
          </div>

          <div class="text-center">
            <div
              class="w-16 h-16 bg-gradient-to-r from-red-500 to-red-700 rounded-full flex items-center justify-center mx-auto mb-4"
            >
              <span class="text-white text-xl font-bold">3</span>
            </div>
            <h3 class="text-xl font-semibold text-neutral-900 mb-2">
              Download
            </h3>
            <p class="text-neutral-600">
              Baixe seu certificado em PDF de alta qualidade.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer class="bg-neutral-900 text-white relative z-10">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
          <!-- Evento Info -->
          <div class="md:col-span-2">
            <div class="flex items-center mb-6">
              <div
                class="bg-gradient-to-r from-red-500 to-red-600 p-3 rounded-xl mr-4"
              >
                <i class="fas fa-heartbeat text-white text-2xl"></i>
              </div>
              <div>
                <h3 class="text-2xl font-bold">Jornada de Cardiologia</h3>
                <p class="text-neutral-400">da Fronteira - Edição 2025</p>
              </div>
            </div>
            <p class="text-neutral-400 mb-4">
              Um evento científico voltado à atualização e valorização dos
              profissionais da saúde cardiovascular, promovido na cidade de Bagé
              - RS.
            </p>
            <div class="flex space-x-4">
              <!-- <div class="bg-red-600 p-2 rounded-lg">
                <i class="fab fa-facebook text-white"></i>
              </div>
              <div class="bg-red-600 p-2 rounded-lg">
                <i class="fab fa-instagram text-white"></i>
              </div>
              <div class="bg-red-600 p-2 rounded-lg">
                <i class="fab fa-linkedin text-white"></i>
              </div> -->
            </div>
          </div>

          <!-- Dados do Evento -->
          <div>
            <h4 class="text-lg font-semibold mb-4 text-red-400">Informações</h4>
            <ul class="space-y-3 text-neutral-400">
              <li class="flex items-center">
                <i class="fas fa-calendar text-red-500 mr-3"></i>
                04-05 Jul 2025
              </li>
              <li class="flex items-center">
                <i class="fas fa-map-marker-alt text-red-500 mr-3"></i>
                Associação Rural de Bagé - RS
              </li>
              <li class="flex items-center">
                <i class="fas fa-users text-red-500 mr-3"></i>
                Profissionais da Saúde
              </li>
            </ul>
          </div>

          <!-- Contato -->
          <div>
            <h4 class="text-lg font-semibold mb-4 text-red-400">Contato</h4>
            <ul class="space-y-3 text-neutral-400">
              <!-- <li class="flex items-center">
                <i class="fas fa-envelope text-red-500 mr-3"></i>
                contato@cardiofronteira.com.br
              </li>
              <li class="flex items-center">
                <i class="fas fa-phone text-red-500 mr-3"></i>
                (53) 99999-9999
              </li> -->
              <li class="flex items-center">
                <i class="fas fa-map-marker-alt text-red-500 mr-3"></i>
                Bagé - RS
              </li>
            </ul>
          </div>
        </div>

        <div class="border-t border-neutral-700 mt-8 pt-8 text-center">
          <div class="flex flex-col md:flex-row justify-between items-center">
            <p class="text-neutral-400">
              &copy; 2025 Jornada de Cardiologia da Fronteira. Todos os direitos
              reservados.
            </p>
            <div class="flex items-center mt-4 md:mt-0">
              <span class="text-neutral-500 text-sm">Desenvolvido com</span>
              <i class="fas fa-heart text-red-500 mx-2 animate-heartbeat"></i>
              <span class="text-neutral-500 text-sm"
                >por <a href="https://github.com/fgalmeida">@fgalmeida</a> &
                <a href="https://github.com/netoe1">@netoe1</a>
                &nbsp;</span
              >
            </div>
          </div>
        </div>
      </div>
    </footer>

    <!-- Scripts -->
    <script>
      // Format CRM input to only numbers
      const crmInput = document.getElementById("crm");
      crmInput.addEventListener("input", function (e) {
        this.value = this.value.replace(/[^0-9]/g, "").slice(0, 6);
      });

      // Smooth scroll for anchor links
      document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
        anchor.addEventListener("click", function (e) {
          e.preventDefault();
          document.querySelector(this.getAttribute("href")).scrollIntoView({
            behavior: "smooth",
          });
        });
      });

      // Add loading state to forms
      document.querySelectorAll("form").forEach((form) => {
        form.addEventListener("submit", function () {
          const submitBtn = form.querySelector('button[type="submit"]');
          if (submitBtn) {
            submitBtn.disabled = true;
            submitBtn.innerHTML =
              '<i class="fas fa-spinner fa-spin mr-2"></i>Processando...';
          }
        });
      });

      // Auto-hide alerts after 5 seconds
      setTimeout(() => {
        const alerts = document.querySelectorAll(".alert-auto-hide");
        alerts.forEach((alert) => {
          alert.style.transition = "opacity 0.5s ease-out";
          alert.style.opacity = "0";
          setTimeout(() => alert.remove(), 500);
        });
      }, 5000);

       // Função para ler parâmetros da query string
  function getQueryParam(name) {
    const urlParams = new URLSearchParams(window.location.search);
    return urlParams.get(name);
  }

  // Mostrar alerta de erro, se houver
  const errorMessage = getQueryParam("error");
  if (errorMessage) {
    const alertBox = document.getElementById("error-alert");
    const messageElement = document.getElementById("error-message");
    messageElement.textContent = decodeURIComponent(errorMessage);
    alertBox.classList.remove("hidden");
  }

  // Mostrar alerta de sucesso, se houver
  const successMessage = getQueryParam("success");
  if (successMessage) {
    const alertBox = document.getElementById("success-alert");
    const messageElement = document.getElementById("success-message");
    messageElement.textContent = decodeURIComponent(successMessage);
    alertBox.classList.remove("hidden");
  }
    </script>
  </body>
</html>
